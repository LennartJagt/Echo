= form_for @animal do |f|
  - if @animal.errors.any?
    #error_explanation
      %h2= "#{pluralize(@animal.errors.count, "error")} prohibited this animal from being saved:"
      %ul
        - @animal.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :specie
    = f.text_field :specie
  .field
    = f.label :owner_last_name
    = f.text_field :owner_last_name
  .actions
    = f.submit 'Save'
